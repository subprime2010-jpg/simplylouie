# ============================================
# SIMPLYLOUIE BACKEND ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env and fill in your values
# cp .env.example .env

# ============================================
# SERVER CONFIGURATION
# ============================================

# Server port (default: 3000)
PORT=3000

# Environment: development | production | test
NODE_ENV=development

# ============================================
# DATABASE
# ============================================

# SQLite database file path (relative to backend directory)
DB_PATH=./data/simplylouie.db

# ============================================
# AUTHENTICATION (JWT)
# ============================================

# Secret key for signing JWT tokens
# IMPORTANT: Generate a strong random string for production
# Example: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# Token expiry duration (default: 7d)
# Format: number + s/m/h/d (seconds/minutes/hours/days)
JWT_EXPIRY=7d

# ============================================
# STRIPE BILLING
# ============================================

# Stripe API secret key (starts with sk_test_ or sk_live_)
# Get from: https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key

# Stripe webhook signing secret (starts with whsec_)
# Get from: https://dashboard.stripe.com/webhooks
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Stripe Price ID for the $2/month subscription
# Get from: https://dashboard.stripe.com/products
STRIPE_PRICE_ID=price_your_price_id

# ============================================
# RATE LIMITING
# ============================================

# Time window for rate limiting in milliseconds (default: 15 minutes)
RATE_LIMIT_WINDOW_MS=900000

# Maximum requests per window (default: 100)
RATE_LIMIT_MAX=100

# ============================================
# OPTIONAL: CORS
# ============================================

# Allowed origins for CORS (comma-separated)
# Leave empty to allow all origins in development
# CORS_ORIGINS=https://simplylouie.com,https://app.simplylouie.com

# ============================================
# OPTIONAL: LOGGING
# ============================================

# Log level: debug | info | warn | error
LOG_LEVEL=info
